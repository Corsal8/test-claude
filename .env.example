# Database
DATABASE_URL=postgresql://dev:dev@localhost:5432/personal_site

# Admin auth — change this to a strong secret in production
ADMIN_PASSWORD=changeme

# Session — generate with: openssl rand -hex 32
SESSION_SECRET=change-this-to-a-random-32-char-secret
